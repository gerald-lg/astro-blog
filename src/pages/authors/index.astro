---
import { getCollection } from "astro:content";
import AuthorCard from "src/components/AuthorCard.astro";
import { siteConfig } from "src/config/site-config";
import MainLayout from "src/layouts/MainLayout.astro";

const authors = await getCollection("author");
---

<MainLayout>
    <div class="container px-6 py-10 mx-auto">
        <h1 class="text-3xl font-semibold text-gray-800 capitalize lg:text-4xl dark:text-white">{siteConfig.title}</h1>
        <h2 class="text-xl font-semibold text-gray-800 capitalize lg:text-2xl dark:text-white">Authors</h2>

        <div class="grid grid-cols-1 gap-8 mt-8 md:mt-16 md:grid-cols-2">
            {
                authors.map((author) => (
                    <AuthorCard 
                        author={author}
                    />
                    
                ))
            }
        </div>
    </div>
</MainLayout>